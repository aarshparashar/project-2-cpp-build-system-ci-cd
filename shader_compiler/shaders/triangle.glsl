// Placeholder shader source — content is irrelevant, since CompileMock does
// not actually parse GLSL. This file exists only to exercise the
// add_custom_command build step end-to-end.
void main() {
    gl_Position = vec4(0.0, 0.0, 0.0, 1.0);
}
